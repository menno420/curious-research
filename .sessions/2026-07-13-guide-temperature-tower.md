# Session — 2026-07-13 — guide-temperature-tower (the print-quality dial)

> **Status:** `complete`
> **📊 Model:** opus-4.8 (Claude Opus family). **Venue:** a dispatched work session for the Curious Research seat, building on the coordinator's baton.

## What this session did

Shipping the **temperature tower guide** under `guides/temperature-tower/` — an animated HTML explainer (`index.html`) that *shows* how a single tower print sweeps nozzle temperature band-by-band so a defect reveals its own fix, a numbered step-by-step `guide.md` that walks the owner from "why print a tower" through slicing, printing, and reading the result, and a one-row index line added to `guides/README.md` so the new explainer is discoverable from the guides lane.

The tower is the first-touch print-quality dial: too hot strings and blobs, too cold under-extrudes and delaminates, and one tower shows the whole band on a single part — a change-one-value-and-look experiment in the house teaching style (CLAUDE.md §1, `docs/teaching-style.md`).

## 💡 Session idea

**A one-page 'tuning tower' index guide** — temperature is the first of a family of calibration towers (retraction/stringing, flow, pressure-advance). A single visual index that links each tower to the symptom it fixes would turn scattered tuning into a decision tree: *see this defect → print this tower*.

## ⟲ Previous-session review

Predecessor: `.sessions/2026-07-13-possibility-dossier.md` (the possibility dossier — a citation-backed, honestly-marked capability map of the bench + Claude, organized as a this-week menu). It handed over a clean tree and a corpus that ends every entry in a try-it experiment; the temperature tower is exactly that shape — the smallest concrete print-quality experiment the owner can run this week — so this session picks up the temperature-tower guide baton and turns "read a tower" into a durable, animated `guides/` trace rather than another `research/` doc.

## Context delta

New surface: `guides/temperature-tower/` (this session creates it) — holding `index.html` (the animated explainer) and `guide.md` (the numbered walkthrough). One new row added to `guides/README.md` indexing the temperature-tower explainer. No kit-owned or `adopt` files touched; this session stays in the `guides/` lane plus this session card and the work claim.
