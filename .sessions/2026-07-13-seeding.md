# Session — 2026-07-13 — repo seeding (the gift is planted)

> **Status:** `complete`
> **📊 Model:** Fable 5 (Claude 5 family). **Venue:** the fleet's hub session (owner-live),
> seeding on the owner's direction.

## What this session did

Seeded `curious-research` from empty (one mock workflow) into a working research companion:

- **substrate-kit v1.15.0 adopted** (`bootstrap.py adopt --wire-enforcement`, guided mode):
  vendored `bootstrap.py` + `LICENSE-substrate-kit` at the tagged release; all interview
  slots answered and docs rendered; the real `substrate-gate` CI workflow regenerated (the
  prior mock workflow's `build` job was banked by the kit at `.substrate/backup/`); the
  auto-merge enabler planted (arms `claude/` branches on green).
- **The teaching doctrine installed** (the owner's founding request): root `CLAUDE.md`
  (house rules: visual teaching, safety hard rules, the loop, public-repo privacy floor) ·
  `docs/teaching-style.md` (binding bar for guides + artifacts) ·
  `.claude/skills/visual-explainers/SKILL.md` (the method).
- **The founding guide shipped**: `guides/how-a-pr-flows/` — a self-contained animated HTML
  explainer (staged animation, captions, replay/step controls, reduced-motion + dark-mode
  aware) + a step-by-step companion with the 3-minute first-PR exercise.
- **The map + rituals**: README gift letter · `docs/git-for-makers.md` ·
  `docs/idea-ritual.md` · `ideas/` seeded with 14 starter one-liners matched to the owner's
  friend's gear (printers/arm/Arduino) + 2 research-first heads.

## 💡 Session idea

A `guides/` **index page as an animated artifact itself** — a single `guides/index.html`
that renders the textbook as a visual map (each guide a node, lighting up as it's added), so
"browse the textbook" is itself a demonstration of the house style. Genuinely useful once
~5 guides exist; parked as `ideas/`-worthy until then (not filed — the backlog should stay
his, not the seeder's).

## ⟲ Previous-session review

First session in this repo — no predecessor to review. Seeding note for the successor: the
kit's `check --strict` findings list was driven to zero *except* items that only a real
session lifecycle closes; this card + the heartbeat stamp close them. The mock workflow
episode is worth remembering: the kit's carve-out scan handled a hand-made workflow
gracefully — trust `adopt` to regenerate kit-owned files rather than editing them.

## Context delta

Everything needed came from the substrate-kit README + the fleet's makerbench blueprint
(idea-engine `ideas/venture-lab/maker-gift-repo-blueprint-2026-07-12.md`) — the seeding
manual this session executed, adapted to the owner's final calls: name `curious-research`,
public, research/teaching emphasis over gift-box framing.
